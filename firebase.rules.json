{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === request.auth.uid",
        ".write": "$uid === request.auth.uid",
        "stocks": {
          ".read": "$uid === request.auth.uid",
          ".write": "$uid === request.auth.uid"
        },
        "investments": {
          ".read": "$uid === request.auth.uid",
          ".write": "$uid === request.auth.uid"
        },
        "pension": {
          ".read": "$uid === request.auth.uid",
          ".write": "$uid === request.auth.uid"
        }
      }
    }
  }
} 