rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      match /stocks/{stockId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /investments/{investmentId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /pension/{pensionId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
} 